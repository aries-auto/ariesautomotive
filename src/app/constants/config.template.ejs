'use strict';

angular.module('ariesautomotive').constant('<%= moduleName %>', {
    <% for(var i=0; i<constants.length; i++){ %>
        '<%= constants[i].name %>': <%= constants[i].value %>,
    <% } %>
});
